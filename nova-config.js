/* ===========================================================
   NOVA workspace configuration
   -----------------------------------------------------------
   Set MODE to 'live' to use Supabase, or 'demo' to run entirely
   in the browser with fake data and no account needed.

   The key below is the publishable key. It is meant to sit in
   the page. What protects your data is the row level security
   in the database, not this key. The secret key from the same
   Supabase page must never appear in any file here.
   =========================================================== */

var NOVA_CONFIG = {
  MODE: 'live',
  SUPABASE_URL: 'https://zufbwysjjtbpklkyscah.supabase.co',
  SUPABASE_KEY: 'sb_publishable_J8j4WCodBuA9_R3DL-yRFg_E0uxlLay',
  SITE_URL: 'https://novaonline.net'
};
